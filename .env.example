# This file serves as an example for environment variables.
# Create a .env file in the root of your project and copy these variables.
# Do NOT commit your actual .env file to version control, especially if it contains sensitive information.

# Example: API Key (replace with your actual key in .env)
# VITE_SOME_API_KEY=your_api_key_here

# Example: Base URL for an API endpoint
# VITE_API_BASE_URL=https://api.example.com

# Example: Debug mode (can be controlled by import.meta.env.DEV in Vite, but useful for other flags)
# VITE_DEBUG_MODE=true
